import { Photographer } from './photographer.model';

describe('Photographer', () => {
  it('should create an instance', () => {
    // @ts-ignore
    expect(new Photographer()).toBeTruthy();
  });
});
